git init
git branch -M main
git remote add origin https://github.com/IvanDarmovis1/first-project.git

git add .
git commit -am "first commit"
git checkout -b blog

git push origin main

git add.
git commit -am "blog changes commit"
git checkout main

git merge blog

git push main
